parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks: 
      composer: ~
      composer_script:
        script: normalize
        triggered_by: [json]
      phpcsfixer2:
        config: .php_cs
      phpunit: ~
